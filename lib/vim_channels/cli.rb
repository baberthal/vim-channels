# frozen_string_literal: true

require "vim_channels"

module VimChannels
  # CLI Runner class.
  class CLI
    # Starts the CLI, using +argv+ as the arguments passed to the script.
    #
    # @param argv [Array<String>]
    def self.start(argv) end
  end
end
